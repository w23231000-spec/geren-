# 直接运行

双击 `HFSS_Optimization_Agent.code-workspace`，或者用 VS Code 打开当前文件夹。

进入左侧“运行和调试”，选择：

- `0 - Presentation Preflight`：演示前只读环境检查；
- `1 - Run Offline Agent`：安全的完全离线验证；
- `2 - Run Bundled Optimizer with MockHFSS`：运行工程内置优化器和等效 S 参数模块；
- `3 - REAL HFSS Full Workflow (Visible AEDT)`：显示界面并启动真实 AEDT/HFSS 完整流程；
- `4 - Run All Tests`：运行 Agent 自动测试。

推荐依次运行 0、4、1、2，确认通过后再运行 3。

真实运行前检查 `runtime_config.json`。真实 HFSS 入口会一直等待两次 Solve 和 S 参数导出完成。
